<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.administrator.notetest">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/notitlebar">
        <activity android:name=".UI.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".UI.Activity_Search"/>

        <activity android:name=".UI.Activity_add"/>

        <activity android:name=".UI.Activity_update"/>

        <provider
            android:authorities="com.example.administrator.notetest"
            android:name=".SQLite.MyContentProvider"/>
    </application>


</manifest>